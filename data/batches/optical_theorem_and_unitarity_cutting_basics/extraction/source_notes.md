# Source Notes: Optical Theorem and Unitarity Cutting Basics

Primary Schwartz coverage pressure for this batch comes from:

- page 867 triage, where `optical theorem` was flagged as a candidate-batch-expansion item
- page 870 triage, where `optical theorem` was again flagged for expansion
- the Stage 7 follow-on queue after the Stage 6 checkpoint

Observed overlap anchors already in the authored graph:

- `qft.scattering_amplitude`
- `qft.asymptotic_one_particle_state`

Batch-shaping decisions:

- keep the batch centered on the theorem-level relation between unitarity and forward
  scattering rather than on broad discontinuity machinery
- include only one reusable cutting-rule node, not a full generalized-cuts branch
- keep process examples and explicit cross-section phenomenology out of scope
