# Source Notes: Axion, Strong CP, and Peccei-Quinn Refinement

Primary Schwartz coverage pressure for this batch comes from:

- page 862 triage, where `axion` remained a direct candidate-new-node item and
  `pseudo-Goldstone` also remained unpromoted
- page 869 triage, where `Peccei-Quinn` remained routed only to the coarse
  symmetry-breaking batch
- the extracted index text, where `strong CP`, `Peccei-Quinn`, and `axion` remain a
  visibly connected late-Standard-Model cluster

Observed overlap anchors already in the authored graph:

- `qcd.nonperturbative_qcd_and_topology`
- `qcd.theta_vacuum`
- `sm.symmetry_breaking_and_standard_model`
- `sm.ssb.spontaneous_symmetry_breaking`
- `sm.ssb.goldstone_boson`
- assumed-background ties to `qft.instanton` and `sm.chiral_anomaly`

Batch-shaping decisions:

- keep the batch centered on the strong-CP / Peccei-Quinn / axion cluster rather than on a
  generic BSM tail
- reuse the existing theta-vacuum branch as the QCD-topology motivation instead of opening
  a second strong-interaction namespace
- keep `sm.pseudo_goldstone_boson` inside this batch because it is the smallest reusable
  bridge that makes the axion payoff legible and also absorbs a remaining Schwartz residue
- treat anomaly and instanton language as assumed background rather than reopening a full
  anomaly-calculation or instanton-mechanics batch
- leave `sm.technicolor` for later explicit deferral or a separate overlap-aware decision,
  since it does not fit coherently into the strong-CP cluster
